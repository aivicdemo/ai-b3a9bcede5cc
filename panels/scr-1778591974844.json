{
  "screenId": "scr-1778591974844",
  "screenName": "予測精度監視画面",
  "html": "<div class=\"prediction-accuracy-monitor\">\n  <!-- ヘッダー部分 -->\n  <div style=\"display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;\">\n    <h1 style=\"color: var(--text); margin: 0; font-size: 24px; font-weight: 600;\">予測精度監視画面</h1>\n    <div style=\"display: flex; gap: 12px;\">\n      <button id=\"btn-detail-analysis\" class=\"action-btn\">詳細分析</button>\n      <button id=\"btn-export-report\" class=\"action-btn secondary\">レポート出力</button>\n    </div>\n  </div>\n\n  <!-- フィルター部分 -->\n  <div class=\"card\" style=\"margin-bottom: 24px; padding: 20px;\">\n    <div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; align-items: end;\">\n      <div>\n        <label style=\"display: block; color: var(--text); margin-bottom: 6px; font-size: 14px;\">期間選択</label>\n        <select id=\"filter-period\" class=\"input\">\n          <option value=\"1month\">過去1ヶ月</option>\n          <option value=\"3months\">過去3ヶ月</option>\n          <option value=\"6months\">過去6ヶ月</option>\n          <option value=\"12months\">過去12ヶ月</option>\n          <option value=\"24months\">過去24ヶ月</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; color: var(--text); margin-bottom: 6px; font-size: 14px;\">店舗選択</label>\n        <select id=\"filter-store\" class=\"input\">\n          <option value=\"\">全店舗</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; color: var(--text); margin-bottom: 6px; font-size: 14px;\">商品カテゴリ</label>\n        <select id=\"filter-category\" class=\"input\">\n          <option value=\"\">全カテゴリ</option>\n          <option value=\"食品\">食品</option>\n          <option value=\"飲料\">飲料</option>\n          <option value=\"日用品\">日用品</option>\n        </select>\n      </div>\n      <div>\n        <button id=\"btn-apply-filter\" class=\"action-btn\">フィルター適用</button>\n      </div>\n    </div>\n  </div>\n\n  <!-- 予測精度サマリーカード -->\n  <div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 16px; margin-bottom: 24px;\">\n    <div class=\"card\" style=\"padding: 20px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">平均予測精度</div>\n      <div id=\"avg-accuracy\" style=\"color: var(--primary); font-size: 32px; font-weight: 600;\">85.2%</div>\n    </div>\n    <div class=\"card\" style=\"padding: 20px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">MAPE</div>\n      <div id=\"mape-value\" style=\"color: var(--text); font-size: 32px; font-weight: 600;\">14.8%</div>\n    </div>\n    <div class=\"card\" style=\"padding: 20px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">RMSE</div>\n      <div id=\"rmse-value\" style=\"color: var(--text); font-size: 32px; font-weight: 600;\">12.3</div>\n    </div>\n    <div class=\"card\" style=\"padding: 20px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">アラート件数</div>\n      <div id=\"alert-count\" style=\"color: #ff6a00; font-size: 32px; font-weight: 600;\">3</div>\n    </div>\n  </div>\n\n  <!-- アラート通知パネル -->\n  <div id=\"alert-panel\" class=\"card\" style=\"margin-bottom: 24px; padding: 16px; border-left: 4px solid #ff6a00; background: var(--bg-card);\">\n    <div style=\"display: flex; align-items: center; gap: 8px; margin-bottom: 12px;\">\n      <span style=\"color: #ff6a00; font-weight: 600;\">⚠ 精度アラート</span>\n    </div>\n    <div id=\"alert-list\" style=\"color: var(--text); font-size: 14px;\">\n      <div style=\"margin-bottom: 8px;\">• 店舗A - 食品カテゴリ: 予測精度 72% (閾値: 80%)</div>\n      <div style=\"margin-bottom: 8px;\">• 店舗B - 飲料カテゴリ: 予測精度 75% (閾値: 80%)</div>\n      <div>• 店舗C - 日用品カテゴリ: 予測精度 78% (閾値: 80%)</div>\n    </div>\n  </div>\n\n  <!-- グラフエリア -->\n  <div style=\"display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-bottom: 24px;\">\n    <!-- 予測精度推移グラフ -->\n    <div class=\"card\" style=\"padding: 20px;\">\n      <h3 style=\"color: var(--text); margin: 0 0 16px 0; font-size: 16px;\">予測精度推移</h3>\n      <div style=\"display: flex; gap: 8px; margin-bottom: 16px;\">\n        <button id=\"chart-period-daily\" class=\"action-btn secondary\" style=\"font-size: 12px; padding: 4px 8px;\">日次</button>\n        <button id=\"chart-period-weekly\" class=\"action-btn\" style=\"font-size: 12px; padding: 4px 8px;\">週次</button>\n        <button id=\"chart-period-monthly\" class=\"action-btn secondary\" style=\"font-size: 12px; padding: 4px 8px;\">月次</button>\n      </div>\n      <canvas id=\"accuracy-trend-chart\" width=\"400\" height=\"200\" style=\"width: 100%; height: 200px;\"></canvas>\n    </div>\n\n    <!-- 予測vs実績比較チャート -->\n    <div class=\"card\" style=\"padding: 20px;\">\n      <h3 style=\"color: var(--text); margin: 0 0 16px 0; font-size: 16px;\">予測vs実績比較</h3>\n      <canvas id=\"prediction-vs-actual-chart\" width=\"400\" height=\"200\" style=\"width: 100%; height: 200px;\"></canvas>\n    </div>\n  </div>\n\n  <!-- 精度ランキング表 -->\n  <div class=\"card\" style=\"padding: 20px;\">\n    <h3 style=\"color: var(--text); margin: 0 0 16px 0; font-size: 16px;\">精度ランキング</h3>\n    <div class=\"table-wrap\">\n      <table>\n        <thead>\n          <tr>\n            <th style=\"cursor: pointer;\" data-sort=\"rank\">順位 ↕</th>\n            <th style=\"cursor: pointer;\" data-sort=\"store\">店舗名 ↕</th>\n            <th style=\"cursor: pointer;\" data-sort=\"category\">カテゴリ ↕</th>\n            <th style=\"cursor: pointer;\" data-sort=\"accuracy\">精度 ↕</th>\n            <th style=\"cursor: pointer;\" data-sort=\"mape\">MAPE ↕</th>\n            <th style=\"cursor: pointer;\" data-sort=\"rmse\">RMSE ↕</th>\n            <th>ステータス</th>\n          </tr>\n        </thead>\n        <tbody id=\"ranking-table-body\">\n        </tbody>\n      </table>\n    </div>\n  </div>\n</div>",
  "initJs": "function(container, apiUrl, tables) {\n  let currentData = [];\n  let currentSort = { column: 'accuracy', direction: 'desc' };\n\n  // 店舗データを店舗選択に追加\n  function populateStoreFilter() {\n    const storeSelect = container.querySelector('#filter-store');\n    const stores = tables[1]?.records || [];\n    stores.forEach(store => {\n      const option = document.createElement('option');\n      option.value = store.店舗ID;\n      option.textContent = store.店舗名;\n      storeSelect.appendChild(option);\n    });\n  }\n\n  // 精度ランキングテーブルの再描画\n  function renderRankingTable(data) {\n    const tbody = container.querySelector('#ranking-table-body');\n    if (!data || data.length === 0) {\n      tbody.innerHTML = '<tr><td colspan=\"7\" style=\"text-align: center; color: var(--text-muted);\">データがありません</td></tr>';\n      return;\n    }\n\n    tbody.innerHTML = data.map((item, index) => `\n      <tr data-id=\"${item.id}\">\n        <td>${index + 1}</td>\n        <td>${item.storeName || ''}</td>\n        <td>${item.category || ''}</td>\n        <td style=\"color: ${item.accuracy >= 80 ? 'var(--primary)' : '#ff6a00'}; font-weight: 600;\">${item.accuracy}%</td>\n        <td>${item.mape}%</td>\n        <td>${item.rmse}</td>\n        <td>\n          <span style=\"padding: 4px 8px; border-radius: 4px; font-size: 12px; background: ${item.accuracy >= 80 ? 'rgba(34, 197, 94, 0.1)' : 'rgba(255, 106, 0, 0.1)'}; color: ${item.accuracy >= 80 ? '#22c562' : '#ff6a00'};\">\n            ${item.accuracy >= 80 ? '正常' : 'アラート'}\n          </span>\n        </td>\n      </tr>\n    `).join('');\n  }\n\n  // 予測精度推移グラフの描画\n  function drawAccuracyTrendChart() {\n    const canvas = container.querySelector('#accuracy-trend-chart');\n    const ctx = canvas.getContext('2d');\n    \n    // Canvas サイズ設定\n    const rect = canvas.getBoundingClientRect();\n    canvas.width = rect.width * window.devicePixelRatio;\n    canvas.height = rect.height * window.devicePixelRatio;\n    ctx.scale(window.devicePixelRatio, window.devicePixelRatio);\n    \n    const width = rect.width;\n    const height = rect.height;\n    const padding = 40;\n    \n    // 背景をクリア\n    ctx.clearRect(0, 0, width, height);\n    \n    // サンプルデータ\n    const data = [\n      { date: '1月', accuracy: 82 },\n      { date: '2月', accuracy: 85 },\n      { date: '3月', accuracy: 78 },\n      { date: '4月', accuracy: 88 },\n      { date: '5月', accuracy: 85 },\n      { date: '6月', accuracy: 90 }\n    ];\n    \n    // 軸の描画\n    ctx.strokeStyle = '#4a5568';\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(padding, padding);\n    ctx.lineTo(padding, height - padding);\n    ctx.lineTo(width - padding, height - padding);\n    ctx.stroke();\n    \n    // データポイントとライン\n    const stepX = (width - padding * 2) / (data.length - 1);\n    const stepY = (height - padding * 2) / 100;\n    \n    ctx.strokeStyle = '#3b82f6';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    \n    data.forEach((point, index) => {\n      const x = padding + index * stepX;\n      const y = height - padding - point.accuracy * stepY;\n      \n      if (index === 0) {\n        ctx.moveTo(x, y);\n      } else {\n        ctx.lineTo(x, y);\n      }\n      \n      // データポイント\n      ctx.fillStyle = '#3b82f6';\n      ctx.beginPath();\n      ctx.arc(x, y, 4, 0, 2 * Math.PI);\n      ctx.fill();\n      \n      // ラベル\n      ctx.fillStyle = '#9fb0d1';\n      ctx.font = '12px sans-serif';\n      ctx.textAlign = 'center';\n      ctx.fillText(point.date, x, height - padding + 20);\n      ctx.fillText(point.accuracy + '%', x, y - 10);\n    });\n    \n    ctx.stroke();\n  }\n\n  // 予測vs実績比較チャートの描画\n  function drawPredictionVsActualChart() {\n    const canvas = container.querySelector('#prediction-vs-actual-chart');\n    const ctx = canvas.getContext('2d');\n    \n    const rect = canvas.getBoundingClientRect();\n    canvas.width = rect.width * window.devicePixelRatio;\n    canvas.height = rect.height * window.devicePixelRatio;\n    ctx.scale(window.devicePixelRatio, window.devicePixelRatio);\n    \n    const width = rect.width;\n    const height = rect.height;\n    const padding = 40;\n    \n    ctx.clearRect(0, 0, width, height);\n    \n    // サンプルデータ\n    const data = [\n      { date: '1日', predicted: 120, actual: 115 },\n      { date: '2日', predicted: 135, actual: 142 },\n      { date: '3日', predicted: 110, actual: 108 },\n      { date: '4日', predicted: 145, actual: 138 },\n      { date: '5日', predicted: 125, actual: 130 },\n      { date: '6日', predicted: 155, actual: 160 }\n    ];\n    \n    // 軸の描画\n    ctx.strokeStyle = '#4a5568';\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(padding, padding);\n    ctx.lineTo(padding, height - padding);\n    ctx.lineTo(width - padding, height - padding);\n    ctx.stroke();\n    \n    const stepX = (width - padding * 2) / (data.length - 1);\n    const maxValue = Math.max(...data.map(d => Math.max(d.predicted, d.actual)));\n    const stepY = (height - padding * 2) / maxValue;\n    \n    // 予測値ライン\n    ctx.strokeStyle = '#3b82f6';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    data.forEach((point, index) => {\n      const x = padding + index * stepX;\n      const y = height - padding - point.predicted * stepY;\n      if (index === 0) ctx.moveTo(x, y);\n      else ctx.lineTo(x, y);\n    });\n    ctx.stroke();\n    \n    // 実績値ライン\n    ctx.strokeStyle = '#22c562';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    data.forEach((point, index) => {\n      const x = padding + index * stepX;\n      const y = height - padding - point.actual * stepY;\n      if (index === 0) ctx.moveTo(x, y);\n      else ctx.lineTo(x, y);\n    });\n    ctx.stroke();\n    \n    // 凡例\n    ctx.fillStyle = '#3b82f6';\n    ctx.fillRect(width - 120, 20, 15, 3);\n    ctx.fillStyle = '#9fb0d1';\n    ctx.font = '12px sans-serif';\n    ctx.fillText('予測値', width - 100, 25);\n    \n    ctx.fillStyle = '#22c562';\n    ctx.fillRect(width - 120, 35, 15, 3);\n    ctx.fillStyle = '#9fb0d1';\n    ctx.fillText('実績値', width - 100, 40);\n  }\n\n  // データの読み込みと表示\n  async function loadData() {\n    try {\n      const predictionData = await window.aivicApi.list(4); // 需要予測結果\n      if (predictionData) {\n        // 精度データの生成（サンプル）\n        currentData = [\n          { id: '1', storeName: '店舗A', category: '食品', accuracy: 85.2, mape: 14.8, rmse: 12.3 },\n          { id: '2', storeName: '店舗B', category: '飲料', accuracy: 78.5, mape: 21.5, rmse: 15.7 },\n          { id: '3', storeName: '店舗C', category: '日用品', accuracy: 92.1, mape: 7.9, rmse: 8.2 },\n          { id: '4', storeName: '店舗D', category: '食品', accuracy: 88.7, mape: 11.3, rmse: 10.1 },\n          { id: '5', storeName: '店舗E', category: '飲料', accuracy: 75.3, mape: 24.7, rmse: 18.9 }\n        ];\n        \n        sortAndRenderData();\n        updateSummaryCards();\n        updateAlertPanel();\n      }\n    } catch (error) {\n      console.error('データ読み込みエラー:', error);\n      renderRankingTable([]);\n    }\n  }\n\n  // サマリーカードの更新\n  function updateSummaryCards() {\n    if (currentData.length === 0) return;\n    \n    const avgAccuracy = currentData.reduce((sum, item) => sum + item.accuracy, 0) / currentData.length;\n    const avgMape = currentData.reduce((sum, item) => sum + item.mape, 0) / currentData.length;\n    const avgRmse = currentData.reduce((sum, item) => sum + item.rmse, 0) / currentData.length;\n    const alertCount = currentData.filter(item => item.accuracy < 80).length;\n    \n    container.querySelector('#avg-accuracy').textContent = avgAccuracy.toFixed(1) + '%';\n    container.querySelector('#mape-value').textContent = avgMape.toFixed(1) + '%';\n    container.querySelector('#rmse-value').textContent = avgRmse.toFixed(1);\n    container.querySelector('#alert-count').textContent = alertCount.toString();\n  }\n\n  // アラートパネルの更新\n  function updateAlertPanel() {\n    const alertItems = currentData.filter(item => item.accuracy < 80);\n    const alertList = container.querySelector('#alert-list');\n    \n    if (alertItems.length === 0) {\n      container.querySelector('#alert-panel').style.display = 'none';\n    } else {\n      container.querySelector('#alert-panel').style.display = 'block';\n      alertList.innerHTML = alertItems.map(item => \n        `<div style=\"margin-bottom: 8px;\">• ${item.storeName} - ${item.category}カテゴリ: 予測精度 ${item.accuracy}% (閾値: 80%)</div>`\n      ).join('');\n    }\n  }\n\n  // ソート機能\n  function sortAndRenderData() {\n    const sortedData = [...currentData].sort((a, b) => {\n      let aVal = a[currentSort.column];\n      let bVal = b[currentSort.column];\n      \n      if (typeof aVal === 'string') {\n        aVal = aVal.toLowerCase();\n        bVal = bVal.toLowerCase();\n      }\n      \n      if (currentSort.direction === 'asc') {\n        return aVal > bVal ? 1 : -1;\n      } else {\n        return aVal < bVal ? 1 : -1;\n      }\n    });\n    \n    renderRankingTable(sortedData);\n  }\n\n  // フィルター適用\n  function applyFilters() {\n    const period = container.querySelector('#filter-period').value;\n    const storeId = container.querySelector('#filter-store').value;\n    const category = container.querySelector('#filter-category').value;\n    \n    let filteredData = [...currentData];\n    \n    if (storeId) {\n      filteredData = filteredData.filter(item => item.storeId === storeId);\n    }\n    \n    if (category) {\n      filteredData = filteredData.filter(item => item.category === category);\n    }\n    \n    renderRankingTable(filteredData);\n    \n    // サマリーカードも更新\n    if (filteredData.length > 0) {\n      const avgAccuracy = filteredData.reduce((sum, item) => sum + item.accuracy, 0) / filteredData.length;\n      const avgMape = filteredData.reduce((sum, item) => sum + item.mape, 0) / filteredData.length;\n      const avgRmse = filteredData.reduce((sum, item) => sum + item.rmse, 0) / filteredData.length;\n      const alertCount = filteredData.filter(item => item.accuracy < 80).length;\n      \n      container.querySelector('#avg-accuracy').textContent = avgAccuracy.toFixed(1) + '%';\n      container.querySelector('#mape-value').textContent = avgMape.toFixed(1) + '%';\n      container.querySelector('#rmse-value').textContent = avgRmse.toFixed(1);\n      container.querySelector('#alert-count').textContent = alertCount.toString();\n    }\n  }\n\n  // CSVエクスポート機能\n  function exportToCsv() {\n    const headers = ['順位', '店舗名', 'カテゴリ', '精度(%)', 'MAPE(%)', 'RMSE'];\n    const csvContent = '\\uFEFF' + [\n      headers.join(','),\n      ...currentData.map((item, index) => [\n        index + 1,\n        `\"${item.storeName}\"`,\n        `\"${item.category}\"`,\n        item.accuracy,\n        item.mape,\n        item.rmse\n      ].join(','))\n    ].join('\\n');\n    \n    const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });\n    const link = document.createElement('a');\n    link.href = URL.createObjectURL(blob);\n    link.download = '予測精度レポート.csv';\n    link.click();\n  }\n\n  // イベントリスナーの設定\n  container.querySelector('#btn-apply-filter').addEventListener('click', applyFilters);\n  \n  container.querySelector('#btn-export-report').addEventListener('click', exportToCsv);\n  \n  container.querySelector('#btn-detail-analysis').addEventListener('click', () => {\n    window.showScreen('scr-1778591838939'); // 需要予測管理画面に遷移\n  });\n\n  // テーブルヘッダーのソート\n  container.addEventListener('click', (e) => {\n    const sortHeader = e.target.closest('[data-sort]');\n    if (sortHeader) {\n      const column = sortHeader.dataset.sort;\n      if (currentSort.column === column) {\n        currentSort.direction = currentSort.direction === 'asc' ? 'desc' : 'asc';\n      } else {\n        currentSort.column = column;\n        currentSort.direction = 'desc';\n      }\n      sortAndRenderData();\n    }\n  });\n\n  // グラフ期間切り替え\n  container.addEventListener('click', (e) => {\n    if (e.target.id.startsWith('chart-period-')) {\n      // ボタンの状態更新\n      container.querySelectorAll('[id^=\"chart-period-\"]').forEach(btn => {\n        btn.className = 'action-btn secondary';\n        btn.style.fontSize = '12px';\n        btn.style.padding = '4px 8px';\n      });\n      e.target.className = 'action-btn';\n      e.target.style.fontSize = '12px';\n      e.target.style.padding = '4px 8px';\n      \n      // グラフ再描画\n      drawAccuracyTrendChart();\n    }\n  });\n\n  // 初期化\n  populateStoreFilter();\n  loadData();\n  \n  // グラフの描画（少し遅延させてcanvasが準備されてから）\n  setTimeout(() => {\n    drawAccuracyTrendChart();\n    drawPredictionVsActualChart();\n  }, 100);\n}"
}