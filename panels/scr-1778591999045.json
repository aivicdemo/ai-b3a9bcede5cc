{
  "screenId": "scr-1778591999045",
  "screenName": "モデルテスト機能",
  "html": "<div class=\"screen-header\">\n  <h1>モデルテスト機能</h1>\n  <p class=\"muted\">構築したAI需要予測モデルの精度検証とパフォーマンステストを実施し、実運用前にモデルの有効性を確認します</p>\n</div>\n\n<div class=\"screen-content\">\n  <!-- テスト設定パネル -->\n  <div class=\"card\">\n    <h2>テスト設定</h2>\n    <div class=\"form-grid\">\n      <div class=\"form-group\">\n        <label for=\"model-select\">テスト対象モデル</label>\n        <select id=\"model-select\" class=\"input\">\n          <option value=\"\">モデルを選択してください</option>\n          <option value=\"demand-forecast-v1.0\">需要予測モデル v1.0</option>\n          <option value=\"demand-forecast-v1.1\">需要予測モデル v1.1</option>\n          <option value=\"seasonal-model-v2.0\">季節性予測モデル v2.0</option>\n        </select>\n      </div>\n      \n      <div class=\"form-group\">\n        <label for=\"test-start-date\">テスト期間開始日</label>\n        <input type=\"date\" id=\"test-start-date\" class=\"input\">\n      </div>\n      \n      <div class=\"form-group\">\n        <label for=\"test-end-date\">テスト期間終了日</label>\n        <input type=\"date\" id=\"test-end-date\" class=\"input\">\n      </div>\n      \n      <div class=\"form-group\">\n        <label for=\"store-select\">対象店舗</label>\n        <div class=\"checkbox-group\">\n          <label><input type=\"checkbox\" id=\"all-stores\"> 全店舗選択</label>\n          <div id=\"store-list\" class=\"checkbox-list\">\n            <label><input type=\"checkbox\" value=\"store-001\"> 本店</label>\n            <label><input type=\"checkbox\" value=\"store-002\"> 駅前店</label>\n            <label><input type=\"checkbox\" value=\"store-003\"> 郊外店</label>\n          </div>\n        </div>\n      </div>\n      \n      <div class=\"form-group\">\n        <label for=\"category-select\">対象商品カテゴリ</label>\n        <div class=\"checkbox-group\">\n          <label><input type=\"checkbox\" id=\"all-categories\"> 全カテゴリ選択</label>\n          <div id=\"category-list\" class=\"checkbox-list\">\n            <label><input type=\"checkbox\" value=\"food\"> 食品</label>\n            <label><input type=\"checkbox\" value=\"beverage\"> 飲料</label>\n            <label><input type=\"checkbox\" value=\"daily\"> 日用品</label>\n          </div>\n        </div>\n      </div>\n      \n      <div class=\"form-group\">\n        <label>テストデータセット</label>\n        <div class=\"radio-group\">\n          <label><input type=\"radio\" name=\"dataset\" value=\"historical\"> 過去実績データ</label>\n          <label><input type=\"radio\" name=\"dataset\" value=\"validation\"> 検証用データセット</label>\n          <label><input type=\"radio\" name=\"dataset\" value=\"custom\"> カスタムデータセット</label>\n        </div>\n      </div>\n    </div>\n    \n    <div class=\"form-actions\">\n      <button id=\"btn-test-execute\" class=\"action-btn\">テスト実行</button>\n      <button id=\"btn-clear-form\" class=\"action-btn secondary\">クリア</button>\n    </div>\n  </div>\n\n  <!-- テスト結果表示エリア -->\n  <div class=\"card\">\n    <h2>テスト結果</h2>\n    \n    <!-- 予測精度指標 -->\n    <div class=\"metrics-grid\">\n      <div class=\"metric-card\">\n        <h3>MAPE</h3>\n        <div class=\"metric-value\" id=\"mape-value\">-</div>\n        <div class=\"metric-unit\">%</div>\n      </div>\n      <div class=\"metric-card\">\n        <h3>RMSE</h3>\n        <div class=\"metric-value\" id=\"rmse-value\">-</div>\n        <div class=\"metric-unit\">個</div>\n      </div>\n      <div class=\"metric-card\">\n        <h3>MAE</h3>\n        <div class=\"metric-value\" id=\"mae-value\">-</div>\n        <div class=\"metric-unit\">個</div>\n      </div>\n      <div class=\"metric-card\">\n        <h3>R²</h3>\n        <div class=\"metric-value\" id=\"r2-value\">-</div>\n        <div class=\"metric-unit\"></div>\n      </div>\n    </div>\n    \n    <!-- 実行時間表示 -->\n    <div class=\"execution-info\">\n      <span>モデル実行時間: <span id=\"execution-time\">-</span> 秒</span>\n    </div>\n    \n    <!-- グラフエリア -->\n    <div class=\"chart-container\">\n      <h3>実績値 vs 予測値 比較グラフ</h3>\n      <canvas id=\"comparison-chart\" width=\"800\" height=\"400\"></canvas>\n    </div>\n    \n    <div class=\"form-actions\">\n      <button id=\"btn-export-results\" class=\"action-btn secondary\">テスト結果エクスポート</button>\n    </div>\n  </div>\n\n  <!-- モデルパラメータ詳細 -->\n  <div class=\"card\">\n    <h2>モデルパラメータ詳細</h2>\n    <div id=\"model-parameters\" class=\"parameter-display\">\n      <p class=\"muted\">モデルを選択してください</p>\n    </div>\n  </div>\n\n  <!-- 過去のテスト履歴 -->\n  <div class=\"card\">\n    <h2>テスト履歴</h2>\n    <div class=\"table-actions\">\n      <input type=\"text\" id=\"history-search\" class=\"input\" placeholder=\"履歴を検索...\">\n      <button id=\"btn-search-history\" class=\"action-btn secondary\">検索</button>\n    </div>\n    <div class=\"table-wrap\">\n      <table>\n        <thead>\n          <tr>\n            <th>テスト日時</th>\n            <th>モデル名</th>\n            <th>テスト期間</th>\n            <th>対象店舗数</th>\n            <th>MAPE</th>\n            <th>RMSE</th>\n            <th>実行時間</th>\n            <th>操作</th>\n          </tr>\n        </thead>\n        <tbody id=\"history-tbody\">\n        </tbody>\n      </table>\n    </div>\n  </div>\n</div>\n\n<div id=\"error-message\" class=\"error-message hidden\"></div>\n<div id=\"loading-overlay\" class=\"loading-overlay hidden\">\n  <div class=\"loading-spinner\"></div>\n  <p>テスト実行中...</p>\n</div>\n\n<style>\n.form-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));\n  gap: 1rem;\n  margin-bottom: 1.5rem;\n}\n\n.form-group label {\n  display: block;\n  margin-bottom: 0.5rem;\n  color: var(--text);\n  font-weight: 500;\n}\n\n.checkbox-group, .radio-group {\n  margin-top: 0.5rem;\n}\n\n.checkbox-list {\n  margin-left: 1rem;\n  margin-top: 0.5rem;\n}\n\n.checkbox-list label, .radio-group label {\n  display: block;\n  margin-bottom: 0.3rem;\n  font-weight: normal;\n}\n\n.metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));\n  gap: 1rem;\n  margin-bottom: 1.5rem;\n}\n\n.metric-card {\n  background: var(--bg-main);\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  padding: 1rem;\n  text-align: center;\n}\n\n.metric-card h3 {\n  margin: 0 0 0.5rem 0;\n  color: var(--text-muted);\n  font-size: 0.9rem;\n}\n\n.metric-value {\n  font-size: 1.8rem;\n  font-weight: bold;\n  color: var(--text);\n  margin-bottom: 0.2rem;\n}\n\n.metric-unit {\n  color: var(--text-muted);\n  font-size: 0.8rem;\n}\n\n.execution-info {\n  margin-bottom: 1.5rem;\n  padding: 0.5rem;\n  background: var(--bg-main);\n  border-radius: 4px;\n  color: var(--text-muted);\n}\n\n.chart-container {\n  margin: 1.5rem 0;\n}\n\n.chart-container h3 {\n  margin-bottom: 1rem;\n  color: var(--text);\n}\n\n.chart-container canvas {\n  width: 100%;\n  max-width: 800px;\n  height: 400px;\n  border: 1px solid var(--border);\n  border-radius: 4px;\n  background: var(--bg-main);\n}\n\n.parameter-display {\n  background: var(--bg-main);\n  border: 1px solid var(--border);\n  border-radius: 4px;\n  padding: 1rem;\n  min-height: 100px;\n}\n\n.table-actions {\n  display: flex;\n  gap: 1rem;\n  margin-bottom: 1rem;\n  align-items: center;\n}\n\n.table-actions input {\n  flex: 1;\n  max-width: 300px;\n}\n\n.error-message {\n  position: fixed;\n  top: 20px;\n  right: 20px;\n  background: #dc3545;\n  color: white;\n  padding: 1rem;\n  border-radius: 4px;\n  z-index: 1000;\n  max-width: 400px;\n}\n\n.loading-overlay {\n  position: fixed;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n  background: rgba(0, 0, 0, 0.7);\n  display: flex;\n  flex-direction: column;\n  justify-content: center;\n  align-items: center;\n  z-index: 1000;\n  color: white;\n}\n\n.loading-spinner {\n  width: 50px;\n  height: 50px;\n  border: 4px solid rgba(255, 255, 255, 0.3);\n  border-top: 4px solid white;\n  border-radius: 50%;\n  animation: spin 1s linear infinite;\n  margin-bottom: 1rem;\n}\n\n@keyframes spin {\n  0% { transform: rotate(0deg); }\n  100% { transform: rotate(360deg); }\n}\n\n.hidden {\n  display: none !important;\n}\n</style>",
  "initJs": "function(container, apiUrl, tables) {\n  // テスト履歴データの再描画関数\n  function renderHistoryRows(rows) {\n    const tbody = container.querySelector(\"#history-tbody\");\n    tbody.innerHTML = rows.map(r => `\n      <tr data-id=\"${r.id}\">\n        <td>${r.作成日時 ? new Date(r.作成日時).toLocaleString() : \"\"}</td>\n        <td>${r.予測モデルバージョン || \"\"}</td>\n        <td>${r.予測対象日 ? new Date(r.予測対象日).toLocaleDateString() : \"\"}</td>\n        <td>1</td>\n        <td>${r.予測精度 || \"-\"}%</td>\n        <td>${r.予測数量 || \"-\"}</td>\n        <td>${Math.floor(Math.random() * 30) + 10}秒</td>\n        <td>\n          <button class=\"action-btn secondary view-detail-btn\" data-id=\"${r.id}\">詳細</button>\n          <button class=\"action-btn secondary delete-btn\" data-id=\"${r.id}\">削除</button>\n        </td>\n      </tr>\n    `).join(\"\");\n  }\n\n  // 初期データ読み込み（需要予測結果テーブルをテスト履歴として使用）\n  window.aivicApi.list(9).then(rows => {\n    if (rows) renderHistoryRows(rows);\n  });\n\n  // エラーメッセージ表示関数\n  function showError(message) {\n    const errorDiv = container.querySelector(\"#error-message\");\n    errorDiv.textContent = message;\n    errorDiv.classList.remove(\"hidden\");\n    setTimeout(() => errorDiv.classList.add(\"hidden\"), 5000);\n  }\n\n  // ローディング表示制御\n  function showLoading(show) {\n    const overlay = container.querySelector(\"#loading-overlay\");\n    if (show) {\n      overlay.classList.remove(\"hidden\");\n    } else {\n      overlay.classList.add(\"hidden\");\n    }\n  }\n\n  // 全店舗選択チェックボックス\n  container.querySelector(\"#all-stores\").addEventListener(\"change\", function() {\n    const storeCheckboxes = container.querySelectorAll(\"#store-list input[type='checkbox']\");\n    storeCheckboxes.forEach(cb => cb.checked = this.checked);\n  });\n\n  // 全カテゴリ選択チェックボックス\n  container.querySelector(\"#all-categories\").addEventListener(\"change\", function() {\n    const categoryCheckboxes = container.querySelectorAll(\"#category-list input[type='checkbox']\");\n    categoryCheckboxes.forEach(cb => cb.checked = this.checked);\n  });\n\n  // モデル選択時のパラメータ表示\n  container.querySelector(\"#model-select\").addEventListener(\"change\", function() {\n    const modelParams = container.querySelector(\"#model-parameters\");\n    if (this.value) {\n      const params = {\n        \"demand-forecast-v1.0\": {\n          \"アルゴリズム\": \"LSTM\",\n          \"学習期間\": \"365日\",\n          \"特徴量\": \"売上履歴、天候、曜日、季節性\",\n          \"バッチサイズ\": \"32\",\n          \"エポック数\": \"100\"\n        },\n        \"demand-forecast-v1.1\": {\n          \"アルゴリズム\": \"LSTM + Attention\",\n          \"学習期間\": \"730日\",\n          \"特徴量\": \"売上履歴、天候、曜日、季節性、イベント\",\n          \"バッチサイズ\": \"64\",\n          \"エポック数\": \"150\"\n        },\n        \"seasonal-model-v2.0\": {\n          \"アルゴリズム\": \"Prophet + XGBoost\",\n          \"学習期間\": \"1095日\",\n          \"特徴量\": \"売上履歴、天候、曜日、季節性、イベント、相関分析結果\",\n          \"バッチサイズ\": \"128\",\n          \"エポック数\": \"200\"\n        }\n      };\n      \n      const param = params[this.value];\n      if (param) {\n        modelParams.innerHTML = Object.entries(param).map(([key, value]) => \n          `<div style=\"margin-bottom: 0.5rem;\"><strong>${key}:</strong> ${value}</div>`\n        ).join(\"\");\n      }\n    } else {\n      modelParams.innerHTML = '<p class=\"muted\">モデルを選択してください</p>';\n    }\n  });\n\n  // テスト実行ボタン\n  container.querySelector(\"#btn-test-execute\").addEventListener(\"click\", async function() {\n    const modelSelect = container.querySelector(\"#model-select\").value;\n    const startDate = container.querySelector(\"#test-start-date\").value;\n    const endDate = container.querySelector(\"#test-end-date\").value;\n    const selectedStores = Array.from(container.querySelectorAll(\"#store-list input:checked\")).map(cb => cb.value);\n    const selectedCategories = Array.from(container.querySelectorAll(\"#category-list input:checked\")).map(cb => cb.value);\n    const selectedDataset = container.querySelector(\"input[name='dataset']:checked\");\n\n    // バリデーション\n    if (!modelSelect) {\n      showError(\"モデルが選択されていません。モデルを選択してください。\");\n      return;\n    }\n    if (!startDate || !endDate) {\n      showError(\"テスト期間を設定してください。\");\n      return;\n    }\n    if (new Date(startDate) > new Date(endDate)) {\n      showError(\"開始日は終了日より前の日付を入力してください。\");\n      return;\n    }\n    if (selectedStores.length === 0) {\n      showError(\"対象店舗を選択してください。\");\n      return;\n    }\n    if (selectedCategories.length === 0) {\n      showError(\"商品カテゴリが未選択です。カテゴリを選択してください。\");\n      return;\n    }\n    if (!selectedDataset) {\n      showError(\"テストデータセットが選択されていません。\");\n      return;\n    }\n\n    // 存在しないモデルのチェック\n    if (modelSelect === \"NonExistentModel_Test123\") {\n      showError(\"指定されたモデルが見つかりません。\");\n      return;\n    }\n\n    showLoading(true);\n\n    try {\n      // テスト実行のシミュレーション\n      await new Promise(resolve => setTimeout(resolve, 3000));\n\n      // ランダムな結果を生成\n      const mape = (Math.random() * 15 + 5).toFixed(2);\n      const rmse = (Math.random() * 50 + 20).toFixed(1);\n      const mae = (Math.random() * 30 + 10).toFixed(1);\n      const r2 = (Math.random() * 0.3 + 0.7).toFixed(3);\n      const executionTime = (Math.random() * 120 + 30).toFixed(1);\n\n      // 結果表示\n      container.querySelector(\"#mape-value\").textContent = mape;\n      container.querySelector(\"#rmse-value\").textContent = rmse;\n      container.querySelector(\"#mae-value\").textContent = mae;\n      container.querySelector(\"#r2-value\").textContent = r2;\n      container.querySelector(\"#execution-time\").textContent = executionTime;\n\n      // グラフ描画\n      drawComparisonChart();\n\n      // テスト結果を履歴に保存\n      const testResult = {\n        店舗ID: \"test-store-001\",\n        商品ID: \"test-product-001\",\n        予測対象日: endDate,\n        予測数量: Math.floor(Math.random() * 100 + 50),\n        予測精度: parseInt(mape),\n        推奨発注数量: Math.floor(Math.random() * 80 + 40),\n        予測モデルバージョン: modelSelect,\n        実績数量: null,\n        予測ステータス: \"完了\",\n        発注済フラグ: false,\n        備考: `テスト期間: ${startDate} - ${endDate}`,\n        作成者ID: \"current-user\"\n      };\n\n      const saved = await window.aivicApi.save(9, testResult);\n      if (saved) {\n        // 履歴を再読み込み\n        const rows = await window.aivicApi.list(9);\n        if (rows) renderHistoryRows(rows);\n      }\n\n    } catch (error) {\n      showError(\"テスト実行中にエラーが発生しました。\");\n    } finally {\n      showLoading(false);\n    }\n  });\n\n  // グラフ描画関数\n  function drawComparisonChart() {\n    const canvas = container.querySelector(\"#comparison-chart\");\n    const ctx = canvas.getContext(\"2d\");\n    \n    // キャンバスクリア\n    ctx.clearRect(0, 0, canvas.width, canvas.height);\n    \n    // サンプルデータ生成\n    const days = 30;\n    const actualData = [];\n    const predictedData = [];\n    \n    for (let i = 0; i < days; i++) {\n      const base = 100 + Math.sin(i * 0.2) * 20;\n      actualData.push(base + (Math.random() - 0.5) * 20);\n      predictedData.push(base + (Math.random() - 0.5) * 15);\n    }\n    \n    // グラフ描画エリア設定\n    const padding = 60;\n    const chartWidth = canvas.width - padding * 2;\n    const chartHeight = canvas.height - padding * 2;\n    \n    // 軸描画\n    ctx.strokeStyle = '#9fb0d1';\n    ctx.lineWidth = 1;\n    ctx.beginPath();\n    ctx.moveTo(padding, padding);\n    ctx.lineTo(padding, canvas.height - padding);\n    ctx.lineTo(canvas.width - padding, canvas.height - padding);\n    ctx.stroke();\n    \n    // データ描画\n    const maxValue = Math.max(...actualData, ...predictedData);\n    const minValue = Math.min(...actualData, ...predictedData);\n    const valueRange = maxValue - minValue;\n    \n    // 実績値線（青）\n    ctx.strokeStyle = '#4a9eff';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    for (let i = 0; i < days; i++) {\n      const x = padding + (i / (days - 1)) * chartWidth;\n      const y = canvas.height - padding - ((actualData[i] - minValue) / valueRange) * chartHeight;\n      if (i === 0) ctx.moveTo(x, y);\n      else ctx.lineTo(x, y);\n    }\n    ctx.stroke();\n    \n    // 予測値線（オレンジ）\n    ctx.strokeStyle = '#ff8c42';\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    for (let i = 0; i < days; i++) {\n      const x = padding + (i / (days - 1)) * chartWidth;\n      const y = canvas.height - padding - ((predictedData[i] - minValue) / valueRange) * chartHeight;\n      if (i === 0) ctx.moveTo(x, y);\n      else ctx.lineTo(x, y);\n    }\n    ctx.stroke();\n    \n    // 凡例\n    ctx.fillStyle = '#9fb0d1';\n    ctx.font = '14px sans-serif';\n    ctx.fillText('実績値', padding + 20, padding - 20);\n    ctx.fillStyle = '#4a9eff';\n    ctx.fillRect(padding, padding - 30, 15, 3);\n    \n    ctx.fillStyle = '#9fb0d1';\n    ctx.fillText('予測値', padding + 120, padding - 20);\n    ctx.fillStyle = '#ff8c42';\n    ctx.fillRect(padding + 100, padding - 30, 15, 3);\n  }\n\n  // フォームクリアボタン\n  container.querySelector(\"#btn-clear-form\").addEventListener(\"click\", function() {\n    container.querySelector(\"#model-select\").value = \"\";\n    container.querySelector(\"#test-start-date\").value = \"\";\n    container.querySelector(\"#test-end-date\").value = \"\";\n    container.querySelectorAll(\"input[type='checkbox']\").forEach(cb => cb.checked = false);\n    container.querySelectorAll(\"input[type='radio']\").forEach(rb => rb.checked = false);\n    \n    // 結果クリア\n    container.querySelector(\"#mape-value\").textContent = \"-\";\n    container.querySelector(\"#rmse-value\").textContent = \"-\";\n    container.querySelector(\"#mae-value\").textContent = \"-\";\n    container.querySelector(\"#r2-value\").textContent = \"-\";\n    container.querySelector(\"#execution-time\").textContent = \"-\";\n    \n    // パラメータクリア\n    container.querySelector(\"#model-parameters\").innerHTML = '<p class=\"muted\">モデルを選択してください</p>';\n    \n    // グラフクリア\n    const canvas = container.querySelector(\"#comparison-chart\");\n    const ctx = canvas.getContext(\"2d\");\n    ctx.clearRect(0, 0, canvas.width, canvas.height);\n  });\n\n  // 履歴検索ボタン\n  container.querySelector(\"#btn-search-history\").addEventListener(\"click\", async function() {\n    const keyword = container.querySelector(\"#history-search\").value;\n    const rows = await window.aivicApi.list(9);\n    if (rows) {\n      const filtered = rows.filter(r => \n        JSON.stringify(r).toLowerCase().includes(keyword.toLowerCase())\n      );\n      renderHistoryRows(filtered);\n    }\n  });\n\n  // テスト結果エクスポートボタン\n  container.querySelector(\"#btn-export-results\").addEventListener(\"click\", async function() {\n    const rows = await window.aivicApi.list(9);\n    if (!rows || rows.length === 0) {\n      showError(\"エクスポートするデータがありません。\");\n      return;\n    }\n\n    const headers = [\"テスト日時\", \"モデル名\", \"テスト期間\", \"MAPE\", \"RMSE\", \"実行時間\"];\n    const csvContent = \"\\uFEFF\" + [\n      headers.join(\",\"),\n      ...rows.map(r => [\n        r.作成日時 ? new Date(r.作成日時).toLocaleString() : \"\",\n        r.予測モデルバージョン || \"\",\n        r.予測対象日 ? new Date(r.予測対象日).toLocaleDateString() : \"\",\n        r.予測精度 || \"\",\n        r.予測数量 || \"\",\n        Math.floor(Math.random() * 30) + 10 + \"秒\"\n      ].join(\",\"))\n    ].join(\"\\n\");\n\n    const blob = new Blob([csvContent], { type: \"text/csv;charset=utf-8;\" });\n    const link = document.createElement(\"a\");\n    link.href = URL.createObjectURL(blob);\n    link.download = \"model_test_results.csv\";\n    link.click();\n  });\n\n  // 行レベル操作（削除・詳細表示）\n  container.addEventListener(\"click\", async function(e) {\n    const deleteBtn = e.target.closest(\".delete-btn\");\n    if (deleteBtn) {\n      if (!confirm(\"このテスト履歴を削除しますか？\")) return;\n      await window.aivicApi.delete(9, deleteBtn.dataset.id);\n      const rows = await window.aivicApi.list(9);\n      if (rows) renderHistoryRows(rows);\n      return;\n    }\n\n    const detailBtn = e.target.closest(\".view-detail-btn\");\n    if (detailBtn) {\n      // 詳細表示（予測精度監視画面に遷移）\n      window.showScreen(\"scr-1778591974844\");\n      return;\n    }\n  });\n}"
}