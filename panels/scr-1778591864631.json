{
  "screenId": "scr-1778591864631",
  "screenName": "データ収集ログ管理",
  "html": "<div class=\"data-collection-log-management\">\n  <!-- ヘッダー部分 -->\n  <div class=\"header-section\">\n    <h1 style=\"color: var(--text); margin: 0 0 24px 0; font-size: 24px; font-weight: 600;\">データ収集ログ管理</h1>\n    \n    <!-- 自動更新設定とアラート通知設定 -->\n    <div class=\"control-panel\" style=\"display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;\">\n      <div class=\"auto-update-section\" style=\"display: flex; align-items: center; gap: 12px;\">\n        <label style=\"color: var(--text); font-weight: 500;\">自動更新:</label>\n        <label class=\"toggle-switch\" style=\"position: relative; display: inline-block; width: 50px; height: 24px;\">\n          <input type=\"checkbox\" id=\"auto-update-toggle\" style=\"opacity: 0; width: 0; height: 0;\">\n          <span class=\"slider\" style=\"position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0; background: var(--border); border-radius: 24px; transition: 0.3s;\"></span>\n        </label>\n      </div>\n      <button id=\"btn-alert-settings\" class=\"action-btn\" style=\"background: var(--primary); color: white; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer;\">\n        アラート通知設定\n      </button>\n    </div>\n  </div>\n\n  <!-- データ収集状況サマリー -->\n  <div class=\"summary-section\" style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; margin-bottom: 24px;\">\n    <div class=\"card summary-card\" style=\"padding: 16px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">総収集件数</div>\n      <div id=\"total-count\" style=\"color: var(--text); font-size: 24px; font-weight: 600;\">0</div>\n    </div>\n    <div class=\"card summary-card\" style=\"padding: 16px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">成功率</div>\n      <div id=\"success-rate\" style=\"color: var(--text); font-size: 24px; font-weight: 600;\">0%</div>\n    </div>\n    <div class=\"card summary-card\" style=\"padding: 16px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">エラー件数</div>\n      <div id=\"error-count\" style=\"color: var(--text); font-size: 24px; font-weight: 600;\">0</div>\n    </div>\n    <div class=\"card summary-card\" style=\"padding: 16px; text-align: center;\">\n      <div style=\"color: var(--text-muted); font-size: 14px; margin-bottom: 8px;\">システム状態</div>\n      <div class=\"status-indicator\" style=\"display: flex; align-items: center; justify-content: center; gap: 8px;\">\n        <div id=\"status-light\" style=\"width: 12px; height: 12px; border-radius: 50%; background: #22c55e;\"></div>\n        <span id=\"status-text\" style=\"color: var(--text); font-weight: 500;\">正常</span>\n      </div>\n    </div>\n  </div>\n\n  <!-- 検索・フィルター部分 -->\n  <div class=\"card filter-section\" style=\"padding: 20px; margin-bottom: 24px;\">\n    <div style=\"display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; margin-bottom: 16px;\">\n      <div>\n        <label style=\"display: block; color: var(--text); font-weight: 500; margin-bottom: 6px;\">データソース</label>\n        <select id=\"filter-data-source\" class=\"input\" style=\"width: 100%;\">\n          <option value=\"\">すべてのデータソース</option>\n          <option value=\"POSシステム\">POSシステム</option>\n          <option value=\"気象庁API\">気象庁API</option>\n          <option value=\"店舗システム\">店舗システム</option>\n          <option value=\"在庫管理システム\">在庫管理システム</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; color: var(--text); font-weight: 500; margin-bottom: 6px;\">ログレベル</label>\n        <select id=\"filter-log-level\" class=\"input\" style=\"width: 100%;\">\n          <option value=\"\">すべてのレベル</option>\n          <option value=\"INFO\">INFO</option>\n          <option value=\"WARNING\">WARNING</option>\n          <option value=\"ERROR\">ERROR</option>\n        </select>\n      </div>\n      <div>\n        <label style=\"display: block; color: var(--text); font-weight: 500; margin-bottom: 6px;\">開始日時</label>\n        <input type=\"datetime-local\" id=\"filter-start-date\" class=\"input\" style=\"width: 100%;\">\n      </div>\n      <div>\n        <label style=\"display: block; color: var(--text); font-weight: 500; margin-bottom: 6px;\">終了日時</label>\n        <input type=\"datetime-local\" id=\"filter-end-date\" class=\"input\" style=\"width: 100%;\">\n      </div>\n    </div>\n    <div style=\"display: flex; gap: 12px;\">\n      <button id=\"btn-search\" class=\"action-btn\" style=\"background: var(--primary); color: white; border: none; padding: 10px 20px; border-radius: 6px; cursor: pointer;\">\n        検索・絞り込み\n      </button>\n      <button id=\"btn-clear-filters\" class=\"action-btn secondary\" style=\"padding: 10px 20px;\">\n        フィルタクリア\n      </button>\n      <button id=\"btn-export\" class=\"action-btn secondary\" style=\"padding: 10px 20px;\">\n        ログエクスポート\n      </button>\n    </div>\n  </div>\n\n  <!-- ログ一覧テーブル -->\n  <div class=\"card\" style=\"padding: 0; overflow: hidden;\">\n    <div class=\"table-wrap\">\n      <table style=\"width: 100%;\">\n        <thead>\n          <tr>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">ログID</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">データ種別</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">データソース</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">収集日時</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">ステータス</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">収集件数</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">エラー件数</th>\n            <th style=\"color: var(--text); padding: 16px; text-align: left; border-bottom: 1px solid var(--border);\">操作</th>\n          </tr>\n        </thead>\n        <tbody id=\"log-table-body\">\n        </tbody>\n      </table>\n    </div>\n  </div>\n\n  <!-- ページネーション -->\n  <div class=\"pagination-section\" style=\"display: flex; justify-content: center; align-items: center; gap: 12px; margin-top: 24px;\">\n    <button id=\"btn-prev-page\" class=\"action-btn secondary\" style=\"padding: 8px 12px;\">前のページ</button>\n    <span id=\"page-info\" style=\"color: var(--text);\">1 / 1</span>\n    <button id=\"btn-next-page\" class=\"action-btn secondary\" style=\"padding: 8px 12px;\">次のページ</button>\n    <select id=\"page-size\" class=\"input\" style=\"margin-left: 16px; width: 100px;\">\n      <option value=\"20\">20件</option>\n      <option value=\"50\">50件</option>\n      <option value=\"100\">100件</option>\n    </select>\n  </div>\n</div>\n\n<!-- ログ詳細モーダル -->\n<div id=\"log-detail-modal\" class=\"modal\" style=\"display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 1000;\">\n  <div class=\"modal-content\" style=\"position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: var(--bg-card); border-radius: 8px; padding: 24px; width: 90%; max-width: 600px; max-height: 80vh; overflow-y: auto;\">\n    <div style=\"display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;\">\n      <h2 style=\"color: var(--text); margin: 0; font-size: 20px; font-weight: 600;\">ログ詳細</h2>\n      <button id=\"btn-close-modal\" style=\"background: none; border: none; color: var(--text); font-size: 24px; cursor: pointer;\">&times;</button>\n    </div>\n    <div id=\"log-detail-content\" style=\"color: var(--text); line-height: 1.6;\">\n    </div>\n  </div>\n</div>\n\n<style>\n.toggle-switch input:checked + .slider {\n  background: var(--primary);\n}\n\n.toggle-switch .slider:before {\n  position: absolute;\n  content: \"\";\n  height: 18px;\n  width: 18px;\n  left: 3px;\n  bottom: 3px;\n  background: white;\n  border-radius: 50%;\n  transition: 0.3s;\n}\n\n.toggle-switch input:checked + .slider:before {\n  transform: translateX(26px);\n}\n\n.modal {\n  animation: fadeIn 0.3s ease-out;\n}\n\n@keyframes fadeIn {\n  from { opacity: 0; }\n  to { opacity: 1; }\n}\n\n.log-row {\n  cursor: pointer;\n  transition: background-color 0.2s;\n}\n\n.log-row:hover {\n  background: rgba(255, 255, 255, 0.05);\n}\n\n.status-badge {\n  padding: 4px 8px;\n  border-radius: 4px;\n  font-size: 12px;\n  font-weight: 500;\n}\n\n.status-success {\n  background: rgba(34, 197, 94, 0.2);\n  color: #22c55e;\n}\n\n.status-error {\n  background: rgba(239, 68, 68, 0.2);\n  color: #ef4444;\n}\n\n.status-warning {\n  background: rgba(245, 158, 11, 0.2);\n  color: #f59e0b;\n}\n\n.status-running {\n  background: rgba(59, 130, 246, 0.2);\n  color: #3b82f6;\n}\n</style>",
  "initJs": "function(container, apiUrl, tables) {\n  let currentPage = 1;\n  let pageSize = 20;\n  let filteredLogs = [];\n  let allLogs = [];\n  let autoUpdateInterval = null;\n\n  // 業務ロジックモジュールのインポート\n  const { \n    collectSalesData, \n    collectWeatherData, \n    collectEventData, \n    collectSeasonalData \n  } = window.aivicLogic || {};\n\n  // データ収集ログのテーブルインデックスを取得\n  const logTableIndex = tables.findIndex(t => t.tableName === 'データ収集ログ');\n  \n  // ステータス表示用の関数\n  function getStatusBadge(status) {\n    const statusMap = {\n      '成功': { class: 'status-success', text: '成功' },\n      '失敗': { class: 'status-error', text: '失敗' },\n      '警告': { class: 'status-warning', text: '警告' },\n      '実行中': { class: 'status-running', text: '実行中' }\n    };\n    const statusInfo = statusMap[status] || { class: 'status-success', text: status };\n    return `<span class=\"status-badge ${statusInfo.class}\">${statusInfo.text}</span>`;\n  }\n\n  // 日時フォーマット関数\n  function formatDateTime(dateStr) {\n    if (!dateStr) return '';\n    const date = new Date(dateStr);\n    return date.toLocaleString('ja-JP', {\n      year: 'numeric',\n      month: '2-digit',\n      day: '2-digit',\n      hour: '2-digit',\n      minute: '2-digit',\n      second: '2-digit'\n    });\n  }\n\n  // テーブル再描画関数\n  function renderRows(logs) {\n    const tbody = container.querySelector('#log-table-body');\n    if (!logs || logs.length === 0) {\n      tbody.innerHTML = '<tr><td colspan=\"8\" style=\"text-align: center; padding: 40px; color: var(--text-muted);\">該当するログが見つかりません</td></tr>';\n      return;\n    }\n\n    const startIndex = (currentPage - 1) * pageSize;\n    const endIndex = startIndex + pageSize;\n    const pageData = logs.slice(startIndex, endIndex);\n\n    tbody.innerHTML = pageData.map(log => `\n      <tr class=\"log-row\" data-id=\"${log.ログID || log.id}\" style=\"border-bottom: 1px solid var(--border);\">\n        <td style=\"padding: 12px 16px; color: var(--text);\">${(log.ログID || log.id || '').substring(0, 8)}...</td>\n        <td style=\"padding: 12px 16px; color: var(--text);\">${log.データ種別 || ''}</td>\n        <td style=\"padding: 12px 16px; color: var(--text);\">${log.データソース || ''}</td>\n        <td style=\"padding: 12px 16px; color: var(--text);\">${formatDateTime(log.収集開始日時)}</td>\n        <td style=\"padding: 12px 16px;\">${getStatusBadge(log.処理ステータス || '成功')}</td>\n        <td style=\"padding: 12px 16px; color: var(--text); text-align: right;\">${log.収集件数 || 0}</td>\n        <td style=\"padding: 12px 16px; color: var(--text); text-align: right;\">${log.エラー件数 || 0}</td>\n        <td style=\"padding: 12px 16px;\">\n          ${log.処理ステータス === '失敗' ? `<button class=\"action-btn secondary retry-btn\" data-id=\"${log.ログID || log.id}\" style=\"margin-right: 8px; padding: 4px 8px; font-size: 12px;\">再実行</button>` : ''}\n          <button class=\"action-btn secondary detail-btn\" data-id=\"${log.ログID || log.id}\" style=\"padding: 4px 8px; font-size: 12px;\">詳細</button>\n        </td>\n      </tr>\n    `).join('');\n\n    updatePagination(logs.length);\n  }\n\n  // ページネーション更新\n  function updatePagination(totalCount) {\n    const totalPages = Math.ceil(totalCount / pageSize);\n    const paginationContainer = container.querySelector('#pagination');\n    \n    if (totalPages <= 1) {\n      paginationContainer.innerHTML = '';\n      return;\n    }\n\n    let paginationHtml = '';\n    \n    // 前へボタン\n    if (currentPage > 1) {\n      paginationHtml += `<button class=\"pagination-btn\" data-page=\"${currentPage - 1}\">前へ</button>`;\n    }\n\n    // ページ番号\n    const startPage = Math.max(1, currentPage - 2);\n    const endPage = Math.min(totalPages, currentPage + 2);\n\n    for (let i = startPage; i <= endPage; i++) {\n      const activeClass = i === currentPage ? 'active' : '';\n      paginationHtml += `<button class=\"pagination-btn ${activeClass}\" data-page=\"${i}\">${i}</button>`;\n    }\n\n    // 次へボタン\n    if (currentPage < totalPages) {\n      paginationHtml += `<button class=\"pagination-btn\" data-page=\"${currentPage + 1}\">次へ</button>`;\n    }\n\n    paginationContainer.innerHTML = paginationHtml;\n  }\n\n  // データ収集実行関数\n  async function executeDataCollection(dataType) {\n    try {\n      let result;\n      const startTime = new Date().toISOString();\n      \n      // ログエントリを作成\n      const logEntry = {\n        ログID: 'log_' + Date.now(),\n        データ種別: dataType,\n        データソース: getDataSource(dataType),\n        収集開始日時: startTime,\n        処理ステータス: '実行中',\n        収集件数: 0,\n        エラー件数: 0\n      };\n\n      // ログテーブルに追加\n      if (logTableIndex >= 0) {\n        await window.aivicApi.save(logTableIndex, logEntry);\n      }\n\n      // 業務ロジック呼び出し\n      switch (dataType) {\n        case '売上データ':\n          result = await collectSalesData();\n          break;\n        case '天候データ':\n          result = await collectWeatherData();\n          break;\n        case 'イベントデータ':\n          result = await collectEventData();\n          break;\n        case '季節データ':\n          result = await collectSeasonalData();\n          break;\n        default:\n          throw new Error(`未対応のデータ種別: ${dataType}`);\n      }\n\n      // 成功時のログ更新\n      logEntry.処理ステータス = '成功';\n      logEntry.収集件数 = result.count || 0;\n      logEntry.収集終了日時 = new Date().toISOString();\n\n      if (logTableIndex >= 0) {\n        await window.aivicApi.update(logTableIndex, logEntry.ログID, logEntry);\n      }\n\n      window.aivicNotify.success(`${dataType}の収集が完了しました（${result.count || 0}件）`);\n      \n    } catch (error) {\n      // エラー時のログ更新\n      const errorLogEntry = {\n        ログID: 'log_' + Date.now(),\n        データ種別: dataType,\n        データソース: getDataSource(dataType),\n        収集開始日時: startTime,\n        処理ステータス: '失敗',\n        収集件数: 0,\n        エラー件数: 1,\n        収集終了日時: new Date().toISOString(),\n        エラー詳細: error.message\n      };\n\n      if (logTableIndex >= 0) {\n        await window.aivicApi.save(logTableIndex, errorLogEntry);\n      }\n\n      window.aivicNotify.error(`${dataType}の収集に失敗しました: ${error.message}`);\n    }\n\n    // ログ一覧を再読み込み\n    await loadLogs();\n  }\n\n  // データソース取得\n  function getDataSource(dataType) {\n    const sourceMap = {\n      '売上データ': 'POSシステム',\n      '天候データ': '気象庁API',\n      'イベントデータ': 'イベント管理システム',\n      '季節データ': 'カレンダーAPI'\n    };\n    return sourceMap[dataType] || '不明';\n  }\n\n  // ログデータ読み込み\n  async function loadLogs() {\n    try {\n      if (logTableIndex >= 0) {\n        const response = await window.aivicApi.list(logTableIndex);\n        allLogs = response.data || [];\n        applyFilters();\n      }\n    } catch (error) {\n      window.aivicNotify.error('ログデータの読み込みに失敗しました: ' + error.message);\n    }\n  }\n\n  // フィルター適用\n  function applyFilters() {\n    const statusFilter = container.querySelector('#status-filter')?.value || '';\n    const dataTypeFilter = container.querySelector('#data-type-filter')?.value || '';\n    const dateFromFilter = container.querySelector('#date-from-filter')?.value || '';\n    const dateToFilter = container.querySelector('#date-to-filter')?.value || '';\n\n    filteredLogs = allLogs.filter(log => {\n      if (statusFilter && log.処理ステータス !== statusFilter) return false;\n      if (dataTypeFilter && log.データ種別 !== dataTypeFilter) return false;\n      if (dateFromFilter && log.収集開始日時 < dateFromFilter) return false;\n      if (dateToFilter && log.収集開始日時 > dateToFilter) return false;\n      return true;\n    });\n\n    currentPage = 1;\n    renderRows(filteredLogs);\n  }\n\n  // 再実行処理\n  async function retryDataCollection(logId) {\n    try {\n      const log = allLogs.find(l => (l.ログID || l.id) === logId);\n      if (!log) {\n        window.aivicNotify.error('対象のログが見つかりません');\n        return;\n      }\n\n      await executeDataCollection(log.データ種別);\n    } catch (error) {\n      window.aivicNotify.error('再実行に失敗しました: ' + error.message);\n    }\n  }\n\n  // 詳細表示\n  function showLogDetail(logId) {\n    const log = allLogs.find(l => (l.ログID || l.id) === logId);\n    if (!log) {\n      window.aivicNotify.error('対象のログが見つかりません');\n      return;\n    }\n\n    const detailHtml = `\n      <div style=\"padding: 20px;\">\n        <h3>ログ詳細</h3>\n        <div style=\"margin: 10px 0;\"><strong>ログID:</strong> ${log.ログID || log.id}</div>\n        <div style=\"margin: 10px 0;\"><strong>データ種別:</strong> ${log.データ種別}</div>\n        <div style=\"margin: 10px 0;\"><strong>データソース:</strong> ${log.データソース}</div>\n        <div style=\"margin: 10px 0;\"><strong>収集開始日時:</strong> ${formatDateTime(log.収集開始日時)}</div>\n        <div style=\"margin: 10px 0;\"><strong>収集終了日時:</strong> ${formatDateTime(log.収集終了日時)}</div>\n        <div style=\"margin: 10px 0;\"><strong>処理ステータス:</strong> ${getStatusBadge(log.処理ステータス)}</div>\n        <div style=\"margin: 10px 0;\"><strong>収集件数:</strong> ${log.収集件数}</div>\n        <div style=\"margin: 10px 0;\"><strong>エラー件数:</strong> ${log.エラー件数}</div>\n        ${log.エラー詳細 ? `<div style=\"margin: 10px 0;\"><strong>エラー詳細:</strong> ${log.エラー詳細}</div>` : ''}\n      </div>\n    `;\n\n    // モーダル表示（簡易実装）\n    const modal = document.createElement('div');\n    modal.style.cssText = 'position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 1000; display: flex; align-items: center; justify-content: center;';\n    \n    const modalContent = document.createElement('div');\n    modalContent.style.cssText = 'background: var(--bg); border-radius: 8px; max-width: 600px; max-height: 80vh; overflow-y: auto;';\n    modalContent.innerHTML = detailHtml + '<div style=\"padding: 20px; text-align: right;\"><button class=\"action-btn primary close-modal\">閉じる</button></div>';\n    \n    modal.appendChild(modalContent);\n    document.body.appendChild(modal);\n\n    modalContent.querySelector('.close-modal').addEventListener('click', () => {\n      document.body.removeChild(modal);\n    });\n\n    modal.addEventListener('click', (e) => {\n      if (e.target === modal) {\n        document.body.removeChild(modal);\n      }\n    });\n  }\n\n  // イベントリスナー設定\n  function setupEventListeners() {\n    // データ収集ボタン\n    const collectSalesBtn = container.querySelector('#collect-sales-btn');\n    const collectWeatherBtn = container.querySelector('#collect-weather-btn');\n    const collectEventBtn = container.querySelector('#collect-event-btn');\n    const collectSeasonalBtn = container.querySelector('#collect-seasonal-btn');\n\n    if (collectSalesBtn) {\n      collectSalesBtn.addEventListener('click', () => executeDataCollection('売上データ'));\n    }\n    if (collectWeatherBtn) {\n      collectWeatherBtn.addEventListener('click', () => executeDataCollection('天候データ'));\n    }\n    if (collectEventBtn) {\n      collectEventBtn.addEventListener('click', () => executeDataCollection('イベントデータ'));\n    }\n    if (collectSeasonalBtn) {\n      collectSeasonalBtn.addEventListener('click', () => executeDataCollection('季節データ'));\n    }\n\n    // フィルター\n    const statusFilter = container.querySelector('#status-filter');\n    const dataTypeFilter = container.querySelector('#data-type-filter');\n    const dateFromFilter = container.querySelector('#date-from-filter');\n    const dateToFilter = container.querySelector('#date-to-filter');\n    const clearFiltersBtn = container.querySelector('#clear-filters-btn');\n\n    if (statusFilter) statusFilter.addEventListener('change', applyFilters);\n    if (dataTypeFilter) dataTypeFilter.addEventListener('change', applyFilters);\n    if (dateFromFilter) dateFromFilter.addEventListener('change', applyFilters);\n    if (dateToFilter) dateToFilter.addEventListener('change', applyFilters);\n\n    if (clearFiltersBtn) {\n      clearFiltersBtn.addEventListener('click', () => {\n        if (statusFilter) statusFilter.value = '';\n        if (dataTypeFilter) dataTypeFilter.value = '';\n        if (dateFromFilter) dateFromFilter.value = '';\n        if (dateToFilter) dateToFilter.value = '';\n        applyFilters();\n      });\n    }\n\n    // 更新ボタン\n    const refreshBtn = container.querySelector('#refresh-btn');\n    if (refreshBtn) {\n      refreshBtn.addEventListener('click', loadLogs);\n    }\n\n    // 自動更新\n    const autoUpdateCheckbox = container.querySelector('#auto-update-checkbox');\n    if (autoUpdateCheckbox) {\n      autoUpdateCheckbox.addEventListener('change', (e) => {\n        if (e.target.checked) {\n          autoUpdateInterval = setInterval(loadLogs, 30000); // 30秒間隔\n        } else {\n          if (autoUpdateInterval) {\n            clearInterval(autoUpdateInterval);\n            autoUpdateInterval = null;\n          }\n        }\n      });\n    }\n\n    // テーブル内のボタンイベント（イベント委譲）\n    const tableBody = container.querySelector('#log-table-body');\n    if (tableBody) {\n      tableBody.addEventListener('click', (e) => {\n        if (e.target.classList.contains('retry-btn')) {\n          const logId = e.target.getAttribute('data-id');\n          retryDataCollection(logId);\n        } else if (e.target.classList.contains('detail-btn')) {\n          const logId = e.target.getAttribute('data-id');\n          showLogDetail(logId);\n        }\n      });\n    }\n\n    // ページネーション\n    const paginationContainer = container.querySelector('#pagination');\n    if (paginationContainer) {\n      paginationContainer.addEventListener('click', (e) => {\n        if (e.target.classList.contains('pagination-btn')) {\n          const page = parseInt(e.target.getAttribute('data-page'));\n          if (page && page !== currentPage) {\n            currentPage = page;\n            renderRows(filteredLogs);\n          }\n        }\n      });\n    }\n  }\n\n  // 初期化\n  async function initialize() {\n    setupEventListeners();\n    await loadLogs();\n  }\n\n  // クリーンアップ\n  function cleanup() {\n    if (autoUpdateInterval) {\n      clearInterval(autoUpdateInterval);\n      autoUpdateInterval = null;\n    }\n  }\n\n  // 初期化実行\n  initialize();\n\n  // クリーンアップ関数を返す\n  return cleanup;\n}",
  "panelHtml": ""
}